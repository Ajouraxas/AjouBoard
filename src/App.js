import ClubIconContainer from "./pageClubList.js"

function App() {
  return (
    <ClubIconContainer />
  );
}

/*import { HashRouter as Router, Routes, Route } from 'react-router-dom';
function App() {
  return (
    <Router>
      <Routes>
        <Route exact path={'/club/:clubId'} element={<ClubPage />}></Route>
      </Routes>
    </Router>
  );
}*/
export default App;
